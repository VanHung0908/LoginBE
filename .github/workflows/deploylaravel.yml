# name: Deploy to VM

# on:
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: self-hosted

#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v2

#     - name: Pull latest code in /var/www/html/LoginBE
#       run: |
#         cd /var/www/html/LoginBE
#         git pull origin main
name: Deploy to Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Setup SSH
      uses: webfactory/ssh-agent@v0.5.3
      with:
        ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

    - name: Add server to known_hosts
      run: ssh-keyscan -H 192.168.4.123 >> ~/.ssh/known_hosts

    - name: Run deployment script
      run: ssh -o StrictHostKeyChecking=no hung@192.168.4.123 'cd /var/www/html/LoginBE && git pull'
